import styled from 'styled-components'

function Search() {
    return <Placeholder>Search — coming soon</Placeholder>
}

export default Search

const Placeholder = styled.p`
    padding: 4rem 2rem;
    color: var(--color-text-tertiary);
    font-size: 14px;
`
